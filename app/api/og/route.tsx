import { ImageResponse } from 'next/og'; export const runtime='edge'; export const alt='One Stupid Quiz — Share Card'; export const size={ width:1200, height:630 }; export const contentType='image/png';
export async function GET(request: Request){ const { searchParams }=new URL(request.url); const points=searchParams.get('points') ?? '0.000'; const label=searchParams.get('label') ?? 'Uniquely Yours'; const date=searchParams.get('date') ?? ''; return new ImageResponse((<div style={{ fontSize:48, background:'#fff', color:'#111', width:'100%', height:'100%', display:'flex', flexDirection:'column', justifyContent:'center', alignItems:'center', border:'16px solid #111' }}><div style={{ fontSize:28, marginBottom:16 }}>onestupidquiz.com</div><div style={{ fontSize:64, fontWeight:800 }}>Stupid Points™: {points}</div><div style={{ fontSize:32, marginTop:8 }}>{label}</div><div style={{ fontSize:24, marginTop:24, color:'#666' }}>{date}</div></div>), { ...size }); }
